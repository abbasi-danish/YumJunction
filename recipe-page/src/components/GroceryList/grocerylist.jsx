import React from 'react';
import AddGroceryItemForm from './addgroceryitem';

const GroceryList = ({ groceryList, handleRemoveItem, handleIncrease, handleDecrease }) => {
    return (
      
    );
  };

export default GroceryList;